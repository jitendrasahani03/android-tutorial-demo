<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MenuBarsExample" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorControlHighlight">@color/grey</item>
        <item name="actionModeStyle">@style/ActionModeTheme</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeCloseDrawable">@drawable/close</item>


    </style>

    <style name="ActionModeTheme" parent="Widget.AppCompat.ActionMode">
        <item name="background">@color/orange</item>
        <item name="titleTextStyle">@style/ActionModeTitleTextStyle</item>
        <item name="subtitleTextStyle">@style/ActionModeSubtitleTitleTextStyle</item>
    </style>

    <style name="ActionModeTitleTextStyle" parent="TextAppearance.AppCompat.Widget.ActionMode.Title">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="ActionModeSubtitleTitleTextStyle" parent="@style/TextAppearance.AppCompat.Widget.ActionMode.Subtitle">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.MenuBarsExample" parent="Base.Theme.MenuBarsExample" />

    <style name="MyToolbarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorSecondary">@color/white
        </item>   <!--Three dots and navigation drawer icon color-->
        <item name="android:textColorPrimary">@color/white
        </item>       <!--toolbar text(title,subtitle,menu item title) color-->
    </style>

    <style name="MyPopupTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">@color/orange</item>
        <item name="android:textColorPrimary">@color/black</item>
    </style>
</resources>